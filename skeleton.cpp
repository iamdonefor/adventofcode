#include "all.h"

using TInput = vector<int>;

TInput parseInput(istream& is) {
    TInput result;

    for (string s; getline(is, s); ) {
    }

    return result;
}

stringstream test{R"(
)"};

int main() {

}
